import * as React from "react";
import CarMap from "../../Components/CarMap";
import Layout from "../Layout";

function Home() {
  return (
    <Layout>
      <CarMap />
    </Layout>
  );
}

export default Home;
